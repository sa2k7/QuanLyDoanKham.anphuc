---
name: javascript
description: "Javascript for QuanLyDoanKham.anphuc. 1 conventions, 2 fixes."
domain: javascript
triggers:
  - glob: "**/*.js"
  - glob: "**/*.jsx"
autoGenerated: true
enabled: true
---

# Javascript

Auto-compiled from **16 real patterns** in **QuanLyDoanKham.anphuc**. This skill is auto-routed to agents when working on javascript files.

## 🔧 Problem Playbooks

### Fixed null crash in Reading
-     const content = require('fs').readFileSync(mdFile, 'utf8');
+     console.log('Reading ' + mdFile);
-     
+     const content = fs.readFileSync(mdFile, 'utf8');
-     // Simple table parser
+     
-     const modules = [];
+     // Simple table parser
-     let currentModule = null;
+     const modules = [];
-     let inTable = false;
+     let currentModule = null;
-     let headers = [];


**Actionable Steps:**
1. Modified 1 files
2. identifier: Reading
3. identifier: Simple
4. identifier: MODULE
5. identifier: Check

### Fixed null crash in QuanLyDoanKham
- const path = require('fs');
+ const path = require('path');
- const mdFile = '../../docs/test_cases.md';
+ const baseDir = 'd:/QuanLyDoanKham';
- const xlsxFile = '../../docs/TEST_CASES.xlsx';
+ const mdFile = path.join(baseDir, 'docs/test_cases.md');
- const docxFile = '../../docs/TEST_CASES.docx';
+ const xlsxFile = path.join(baseDir, 'docs/TEST_CASES.xlsx');
- 
+ const docxFile = path.join(ba

**Actionable Steps:**
1. Modified 1 files
2. identifier: QuanLyDoanKham
3. identifier: Simple
4. identifier: MODULE
5. identifier: Check

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Fixed null crash in Exporting — confirmed 5x** — - const mdFile = path.join(baseDir, 'docs/test_cases.md');
+ const tcMd = path.join(baseDir, 'docs/t

## 🤔 Decisions & Trade-offs

- **trade-off in hardening_audit.js** — File updated (external): scripts/hardening_audit.js

Content summary (54 lines):
const fs = require(

---
*Auto-generated by BrainSync 🧠 | 16 patterns | 2026-04-09*

---
name: vue
description: "Vue for QuanLyDoanKham.anphuc. 11 conventions, 22 fixes."
domain: vue
triggers:
  - glob: "**/*.vue"
autoGenerated: true
enabled: true
---

# Vue

Auto-compiled from **58 real patterns** in **QuanLyDoanKham.anphuc**. This skill is auto-routed to agents when working on vue files.

## 🔧 Problem Playbooks

### Fixed null crash in DoanKham — reduces initial bundle size with code splitting
-           <span class="text-slate-200 ml-2 font-black">/</span>
+         </h2>
-           <span class="text-primary font-black tabular-nums">{{ String(groups.length).padStart(3, '0') }}</span>
+         <p class="text-slate-400 font-black uppercase tracking-[0.3em] text-[9px] mt-2">{{ i18n.t('groups.subtitle') }}</p>
-         </h2>
+       </div>
-         <p class="text-slate-400 font-black 

**Actionable Steps:**
1. Modified 1 files
2. identifier: DoanKham
3. identifier: Create
4. identifier: Download
5. identifier: Plus

### Fixed null crash in KHAI — prevents null/undefined runtime crashes
-             <div class="lg:col-span-3 flex justify-end pt-2">
+             <div class="lg:col-span-3 flex justify-end pt-2 gap-4">
-                  <button type="submit" class="btn-premium bg-gradient-to-r from-teal-500 to-emerald-500 text-white px-12 py-4 rounded-xl shadow-lg shadow-teal-500/30 hover:shadow-teal-500/50 hover:-translate-y-1 transition-all font-black">XÁC NHẬN KHAI BÁO</butt

**Actionable Steps:**
1. Modified 1 files
2. identifier: KHAI
3. identifier: Search
4. identifier: List
5. identifier: String

### Fixed null crash in Global — prevents null/undefined runtime crashes
-     const map = { company: 'Cơ quan/Đối tác', contract: 'Hợp đồng pháp lý', staff: 'Nhân sự y tế', group: 'Đoàn đang vận hành' }
+ const getResTypeName = (type) => {
-     return map[type] || 'Dữ liệu hệ thống'
+     const map = { company: 'Cơ quan/Đối tác', contract: 'Hợp đồng pháp lý', staff: 'Nhân sự y tế', group: 'Đoàn đang vận hành' }
- }
+     return map[type] || 'Dữ liệu hệ thống'
-

**Actionable Steps:**
1. Modified 1 files
2. identifier: Global
3. identifier: Keyboard
4. identifier: Shortcut
5. identifier: Ctrl

### Fixed null crash in ConfirmDialog — prevents null/undefined runtime crashes
- import ConfirmDialog from '../components/ConfirmDialog.vue'
+ import { useI18nStore } from '../stores/i18n'
- 
+ import ConfirmDialog from '../components/ConfirmDialog.vue'
- const authStore = useAuthStore()
+ 
- const { can } = usePermission()
+ const authStore = useAuthStore()
- const toast = useToast()
+ const i18n = useI18nStore()
- const groups = ref([])
+ const { can } = usePerm

**Actionable Steps:**
1. Modified 1 files
2. identifier: ConfirmDialog
3. identifier: Open
4. identifier: Date
5. identifier: Partial

### Fixed null crash in Minh — prevents null/undefined runtime crashes
-               <div class="space-y-3">
+                 <label class="text-[10px] font-black uppercase tracking-[0.3em] text-slate-400 ml-1">Địa chỉ (Tỉnh/TP)</label>
-                 <label class="text-[10px] font-black uppercase tracking-[0.3em] text-slate-400 ml-1">Địa chỉ (Tỉnh/TP)</label>
+                 <input v-model="form.address" type="text" class="w-full px-6 py-4 bg-slate-50 border

**Actionable Steps:**
1. Modified 1 files
2. identifier: Minh
3. identifier: ANG
4. identifier: THAY
5. identifier: Teleport

### Fixed null crash in CurrencyInput — prevents null/undefined runtime crashes
-             <div class="flex flex-col gap-2">
+                     <div class="flex flex-col gap-2">
-                 <label class="text-[10px] font-black uppercase tracking-widest text-slate-400 ml-1">Đơn giá (VNĐ/Người)</label>
+                         <label class="text-[10px] font-black uppercase tracking-widest text-slate-400 ml-1">Tên hợp đồng / Ghi chú nhanh</label>
-              

**Actionable Steps:**
1. Modified 1 files
2. identifier: Ghi
3. identifier: CurrencyInput
4. identifier: Tab
5. identifier: Filter

### Fixed null crash in Date
-                                 <h3 class="text-2xl font-black text-slate-800">[HĐ-{{ detailsModal.data.healthContractId }}] {{ detailsModal.data.shortName || detailsModal.data.companyName }}</h3>
+                                 <h3 class="text-2xl font-black text-slate-800">{{ detailsModal.data.contractName || 'Chi tiết hợp đồng' }}</h3>
-                                 <p class="text-[10p

**Actionable Steps:**
1. Modified 1 files
2. identifier: Chi
3. identifier: Date
4. identifier: Pending

### Fixed null crash in Thao — prevents null/undefined runtime crashes
-                 <th class="px-8 py-8 text-left text-[10px] font-black uppercase tracking-[0.2em] text-slate-400">Năm sinh / Giới tính</th>
+                 <th class="px-8 py-8 text-left text-[10px] font-black uppercase tracking-[0.2em] text-slate-400">Mã NV / Phòng ban</th>
-                 <th class="px-8 py-8 text-left text-[10px] font-black uppercase tracking-[0.2em] text-slate-400">Liên l

**Actionable Steps:**
1. Modified 1 files
2. identifier: Thao
3. identifier: CMND
4. identifier: Nam
5. identifier: Unknown

### Fixed null crash in Minh — prevents null/undefined runtime crashes
-                 <label class="text-[10px] font-black uppercase tracking-[0.3em] text-slate-400 ml-1">Địa chỉ (Tỉnh/TP)</label>
+               <div class="space-y-3">
-                 <input v-model="form.address" type="text" class="w-full px-6 py-4 bg-slate-50 border-2 border-transparent focus:border-pink-500/20 focus:bg-white rounded-2xl outline-none font-bold text-slate-700 transition-all" p

**Actionable Steps:**
1. Modified 1 files
2. identifier: Minh
3. identifier: ANG
4. identifier: THAY
5. identifier: Teleport

### Patched security issue Clinic — parallelizes async operations for speed
-               
+               <Clinic v-if="activeMenu === 'clinic'" />
-               <div v-if="!['companies', 'contracts', 'staff', 'groups', 'supplies', 'users', 'permissions', 'payroll', 'departments', 'my-schedule', 'patients', 'analytics'].includes(activeMenu)" class="flex flex-col items-center justify-center py-40 bg-white rounded-[4rem] border-4 border-dashed border-slate-50">
+   

**Actionable Steps:**
1. Modified 1 files
2. identifier: Clinic
3. identifier: SettlementReport
4. identifier: Module
5. identifier: Quay

### Fixed null crash in Building — parallelizes async operations for speed
-   Building, ShieldCheck, CalendarCheck, Menu, UserRound
+   Building, ShieldCheck, CalendarCheck, Menu, UserRound, Activity, Calculator, History
- 
+ import Clinic from './Clinic.vue'
- 
+ import SettlementReport from './SettlementReport.vue'
- const { can } = usePermission()
+ 
- const getRoleDisplayName = (role) => {
+ const { can } = usePermission()
-   return i18n.t('roles.' + role

**Actionable Steps:**
1. Modified 1 files
2. identifier: Building
3. identifier: ShieldCheck
4. identifier: CalendarCheck
5. identifier: Menu

### Fixed null crash in Edit — prevents null/undefined runtime crashes
-   Trash2, Edit, History, X, Phone, User as UserIcon 
+   Trash2, Edit, History, X, Phone, User as UserIcon,
- } from 'lucide-vue-next'
+   Calendar, ArrowRight
- import api from '../services/apiClient'
+ } from 'lucide-vue-next'
- import { useToast } from '../composables/useToast'
+ import api from '../services/apiClient'
- 
+ import { useToast } from '../composables/useToast'
- const toast = us

**Actionable Steps:**
1. Modified 1 files
2. identifier: Edit
3. identifier: History
4. identifier: Phone
5. identifier: User

### Fixed null crash in History — prevents null/undefined runtime crashes
-     </Teleport>
+     <!-- History Modal -->
-   </div>
+     <Teleport to="body">
- </template>
+       <div v-if="showHistoryModal" class="fixed inset-0 z-[1000] flex items-center justify-center p-6 bg-slate-900/60 backdrop-blur-xl animate-fade-in">
- 
+         <div class="bg-white w-full max-w-4xl rounded-[3rem] shadow-2xl overflow-hidden animate-scale-up border-4 border-slate-50 flex flex-c

**Actionable Steps:**
1. Modified 1 files
2. identifier: History
3. identifier: Modal
4. identifier: Teleport
5. identifier: Calendar

### Fixed null crash in Modal — prevents null/undefined runtime crashes
- 
+ import { useToast } from '../composables/useToast'
- const patients = ref([])
+ 
- const loading = ref(false)
+ const toast = useToast()
- const saving = ref(false)
+ 
- const searchQuery = ref('')
+ const patients = ref([])
- 
+ const loading = ref(false)
- // Modal state
+ const saving = ref(false)
- const showModal = ref(false)
+ const searchQuery = ref('')
- const isEdit = ref(false)
+ 
-

**Actionable Steps:**
1. Modified 1 files
2. identifier: Modal
3. identifier: History
4. identifier: Nam
5. identifier: Patients

### Fixed null crash in Header — prevents null/undefined runtime crashes
-   <div class="space-y-6">
+   <div class="space-y-6 max-w-[1600px] mx-auto p-4 sm:p-8 animate-fade-in">
-     <!-- Header -->
+     <!-- Header Section -->
-     <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center space-y-4 sm:space-y-0">
+     <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center gap-6">
-       <div>
+       <div class=

**Actionable Steps:**
1. Modified 1 files
2. identifier: Header
3. identifier: Section
4. identifier: UserRound
5. identifier: Theo

### Fixed null crash in Patients — parallelizes async operations for speed
- 
+ import Patients from './Patients.vue'
- const { can } = usePermission()
+ 
- const getRoleDisplayName = (role) => {
+ const { can } = usePermission()
-   return i18n.t('roles.' + role)
+ 
- }
+ const getRoleDisplayName = (role) => {
- 
+   return i18n.t('roles.' + role)
- const getRoleBadgeClass = (role) => {
+ }
-     switch(role) {
+ 
-         case 'Admin': return 'bg-slate

**Actionable Steps:**
1. Modified 1 files
2. identifier: Patients
3. identifier: Admin
4. identifier: MedicalGroupManager
5. identifier: PersonnelManager

### Patched security issue Patients — parallelizes async operations for speed
-               <MySchedule v-if="activeMenu === 'my-schedule'" />
+               <Patients v-if="activeMenu === 'patients'" />
-               
+               <MySchedule v-if="activeMenu === 'my-schedule'" />
-               <div v-if="!['companies', 'contracts', 'staff', 'groups', 'supplies', 'users', 'permissions', 'payroll', 'departments', 'my-schedule'].includes(activeMenu)" class="fle

**Actionable Steps:**
1. Modified 1 files
2. identifier: Patients
3. identifier: MySchedule
4. identifier: Module
5. identifier: Quay

### Fixed null crash in Patients — parallelizes async operations for speed
-     { id: 'staff', name: i18n.locale === 'vi' ? 'Nhân sự' : 'Staff', icon: UsersIcon, color: 'bg-rose-50 text-rose-600', desc: i18n.locale === 'vi' ? 'Đội ngũ Y bác sĩ & Vận hành.' : 'Medical team & operations.', permission: 'NhanSu.View' },
+     { id: 'patients', name: i18n.locale === 'vi' ? 'Bệnh Nhân' : 'Patients', icon: UserRound, color: 'bg-pink-50 text-pink-600', desc: i18n.locale === 'v

**Actionable Steps:**
1. Modified 1 files
2. identifier: Patients
3. identifier: UserRound
4. identifier: Patient
5. identifier: DoanKham

### Fixed null crash in Stethoscope — reduces initial bundle size with code split...
-         <div v-for="group in filteredGroups" :key="group.groupId" 
+         <div v-if="filteredGroups.length === 0" class="premium-card bg-white rounded-[2rem] shadow-xl border border-slate-100 p-16 text-center flex flex-col items-center justify-center">
-              class="premium-card bg-white rounded-[2rem] shadow-xl border border-slate-100 overflow-hidden group/card">
+             <di

**Actionable Steps:**
1. Modified 1 files
2. identifier: Stethoscope
3. identifier: Open
4. identifier: DoanKham
5. identifier: Edit

### Fixed null crash in CurrencyI

... [Truncated — see individual observations for full content]

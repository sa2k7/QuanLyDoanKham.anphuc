# Learned Patterns — QuanLyDoanKham.anphuc
> 50 patterns extracted from your coding history.

## gotcha

- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.pdb (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Updated API endpoint Failed (seen 4x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Updated API endpoint Failed (seen 5x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.pdb (seen 4x)
- **Pattern**: Avoid: gotcha in rjsmrazor.dswa.cache.json (seen 2x)
- **Pattern**: Avoid: gotcha in rjsmcshtml.dswa.cache.json (seen 2x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.AssemblyInfo.cs (seen 2x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.sourcelink.json (seen 2x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.pdb (seen 5x)
- **Pattern**: Avoid: gotcha in rjsmcshtml.dswa.cache.json (seen 3x)
- **Pattern**: Avoid: gotcha in rjsmrazor.dswa.cache.json (seen 3x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.pdb (seen 6x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.pdb (seen 8x)
- **Pattern**: Avoid: gotcha in rjsmrazor.dswa.cache.json (seen 4x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.pdb (seen 10x)
- **Pattern**: Avoid: gotcha in rjsmrazor.dswa.cache.json (seen 5x)
- **Pattern**: Avoid: gotcha in temp_build.log (seen 2x)
- **Pattern**: Avoid: gotcha in build.log (seen 3x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.csproj.CoreCompileInputs.cache (seen 2x)
- **Pattern**: Avoid: gotcha in rjsmcshtml.dswa.cache.json (seen 4x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Replaced auth Build (seen 20x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.AssemblyInfo.cs (seen 3x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.MvcApplicationPartsAssemblyInfo.cs (seen 2x)
- **Pattern**: Avoid: gotcha in rpswa.dswa.cache.json (seen 2x)
- **Pattern**: Avoid: gotcha in QuanLyDoanKham.API.sourcelink.json (seen 3x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Updated API endpoint Failed (seen 8x)

## convention

- **Pattern**: Always: Optimized BSJB — uses a proper password hashing algorithm — confirmed 5x (seen 2x)
- **Pattern**: Always: Optimized GlobalPropertiesHash — confirmed 4x (seen 3x)
- **Pattern**: Always: Optimized BSJB — uses a proper password hashing algorithm — confirmed 5x (seen 3x)
- **Pattern**: Always: Optimized BSJB — uses a proper password hashing algorithm — confirmed 4x (seen 4x)
- **Pattern**: Always: Optimized GlobalPropertiesHash — confirmed 4x (seen 2x)
- **Pattern**: Always: Optimized GlobalPropertiesHash — confirmed 8x (seen 4x)
- **Pattern**: Always: Replaced auth Mozilla — hardens HTTP security headers — confirmed 4x (seen 2x)
- **Pattern**: Always: problem-fix in 9e156dadfc5dbd714a5e-c74fbf5bd2ac87f0a2b3-retry1.trace — confirmed 3x (seen 2x)
- **Pattern**: Always: [CLAUDE.md] **NEVER** run `git clean -fd` or `git reset --hard` without checking `git log` and verifying commits exist. (seen 2x)
- **Pattern**: Always: [CLAUDE.md] **NEVER** delete untracked files or folders blindly. Always backup or stash before bulk edits. (seen 2x)
- **Pattern**: Always: [CLAUDE.md] **Don't mix Tailwind with inline styles** — Don't mix Tailwind with inline styles (seen 2x)
- **Pattern**: Always: [CLAUDE.md] **Don't mutate props directly — emit events to parent** — Don't mutate props directly — emit events to parent (seen 2x)
- **Pattern**: Always: [CLAUDE.md] **Always use :key with v-for directives** — Always use :key with v-for directives (seen 2x)
- **Pattern**: Always: [CLAUDE.md] **Don't mix CommonJS (require) and ESM (import) in same project** — Don't mix CommonJS (require) and ESM (import) in same project (seen 2x)
- **Pattern**: Always: [CLAUDE.md] Extract repeated class patterns into components (seen 3x)
- **Pattern**: Always: [CLAUDE.md] Use responsive prefixes consistently (sm:, md:, lg:, xl:) (seen 3x)
- **Pattern**: Always: [CLAUDE.md] Don't use arbitrary values when a utility class exists (seen 3x)
- **Pattern**: Always: [CLAUDE.md] Use computed properties for derived state (seen 3x)
- **Pattern**: Always: [CLAUDE.md] Use Composition API (setup) for new components (seen 3x)
- **Pattern**: Always: [CLAUDE.md] Enable strict mode in tsconfig.json (seen 3x)
- **Pattern**: Always: [CLAUDE.md] Prefer Array methods (map, filter, reduce) over manual loops (seen 3x)
- **Pattern**: Always: [CLAUDE.md] Use template literals for string interpolation, not concatenation (seen 3x)

## decision

- **Pattern**: Decision: Optimized BSJB — uses a proper password hashing algorithm (seen 2x)

## problem-fix

- **Pattern**: When encountering this, fix by: problem-fix in build_log.txt (seen 2x)

